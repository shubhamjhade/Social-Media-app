<!DOCTYPE html>
<html>
<head>
    <title>Owner Dashboard</title>
    <style>
        body { font-family: sans-serif; background: #f0f2f5; padding: 20px; }
        .container { max-width: 800px; margin: 0 auto; background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th, td { border: 1px solid #ddd; padding: 10px; text-align: left; }
        th { background-color: #f2f2f2; }
        .btn-approve { background: #42b72a; color: white; padding: 5px 10px; text-decoration: none; border: none; cursor: pointer; border-radius: 4px;}
        .section-title { color: #1877f2; border-bottom: 2px solid #1877f2; padding-bottom: 5px; margin-top: 30px; }
        .nav { margin-bottom: 20px; }
    </style>
</head>
<body>

    <div class="nav">
        <a href="/">← Back to Home Feed</a>
    </div>

    <div class="container">
        <h1>👑 Owner Dashboard</h1>

        <h2 class="section-title">🚨 Pending Requests</h2>
        <% if(pendingUsers.length === 0) { %>
            <p>No pending requests.</p>
        <% } else { %>
            <table>
                <tr>
                    <th>Full Name</th>
                    <th>Details (Col ID / Mob)</th>
                    <th>Username</th>
                    <th>Action</th>
                </tr>
                <% pendingUsers.forEach(user => { %>
                    <tr>
                        <td><%= user.fullName %></td>
                        <td>ID: <%= user.collegeId %><br>Mob: <%= user.mobile %></td>
                        <td><%= user.username %></td>
                        <td>
                            <form action="/admin/approve/<%= user._id %>" method="POST">
                                <button type="submit" class="btn-approve">✅ Approve</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </table>
        <% } %>

        <h2 class="section-title">👥 Active Users</h2>
        <ul>
            <% activeUsers.forEach(user => { %>
                <li><%= user.fullName %> (@<%= user.username %>)</li>
            <% }) %>
        </ul>

    </div>

</body>
</html>